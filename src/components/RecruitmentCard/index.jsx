import { CalendarDays, MapPin, Users } from "lucide-react";
import { Link } from "react-router-dom";
import { openSlots } from "@/data/recruitment";
export default function RecruitmentCard({post}){const slots=openSlots(post);const type=post.recruitmentFor==="Coaches"?"coach":"player";return <article className="recruitment-card"><div className="recruitment-team"><span>{post.teamLogo?<img src={post.teamLogo} alt=""/>:post.teamName.slice(0,2).toUpperCase()}</span><div><small>{post.game} · {type} recruitment</small><strong>{post.teamName}</strong></div><b className={`recruitment-status status-${post.status.toLowerCase()}`}>{post.status}</b></div><h2>{post.title}</h2><div className="recruitment-card-meta"><span><Users/>{slots} {type}{slots===1?"":"es"} needed</span>{post.region&&<span><MapPin/>{post.region}</span>}<span><CalendarDays/>{new Date(post.createdAt).toLocaleDateString("en-NG",{day:"numeric",month:"short",year:"numeric"})}</span></div><div className="recruitment-role-list">{post.roles.map(role=><span key={role}>{role}</span>)}</div>{post.tournament&&<p className="recruitment-tournament">Recruiting for: <strong>{post.tournament}</strong></p>}<Link className="button button-full" to={`/recruitment/${post.id}`}>View Recruitment</Link></article>}
